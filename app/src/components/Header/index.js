import { Container } from './styles';

function Header() {
  return (
    <Container>
      <a href={'/'}>CAPJu</a>

      <a href={'/Login'}>Login</a>
    </Container>
  );
}

export default Header;
